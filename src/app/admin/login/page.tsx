import AdminLogin from '@/components/Admin/AdminLogin';
import React from 'react';

const page = () => {
    return (
        <div>
            <AdminLogin />
        </div>
    );
};

export default page;